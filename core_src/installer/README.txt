jArmyTool installation intructions.

System requirements
You need to have Java 1.4 or later installed on your system. It can 
be downloaded free from java.com.

Windows
Start the installer by starting the install.bat

Linux
Start the installer by starting the install.sh

Other systems.
Start installer by running the installer jar manually by command:
java -jar jArmyToolInstaller.jar